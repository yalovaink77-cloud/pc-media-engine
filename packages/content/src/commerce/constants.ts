/** Default maximum YAML file size (1 MB). */
export const DEFAULT_MAX_YAML_FILE_BYTES = 1024 * 1024;

/** Default maximum YAML alias expansions during parsing. */
export const DEFAULT_YAML_MAX_ALIAS_COUNT = 100;
